<%= form_with(model: deposit, local: true) do |form| %>
  <% if deposit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deposit.errors.count, "error") %> prohibited this deposit from being saved:</h2>

      <ul>
      <% deposit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field :user_id, id: :deposit_user_id, :value => current_user.id %>
  </div>

  <div class="field">
    <%= form.hidden_field :date, id: :deposit_date,:value => Date.today %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :deposit_amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
